# deed-hunter-cruise

Vite + React + TailwindCSS site generated by ClawBot Webdesigner.

## Run

```bash
bun install   # or: npm install
bun run dev   # or: npm run dev
```

Then open <http://localhost:4200>.

## Build

```bash
bun run build
```

Output is in `dist/`.
